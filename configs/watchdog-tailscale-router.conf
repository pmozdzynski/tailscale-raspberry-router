# Managed by tailscale-raspberry-router
watchdog-device = /dev/watchdog
watchdog-timeout = 60
interval = 10

# Read-only check (no systemctl restart / sysctl writes)
test-binary = /usr/local/bin/router-watchdog-test.sh
test-timeout = 30
